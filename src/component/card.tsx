export default function Card({
  title,
  description,
  color,
  icon,
}: {
  title: string;
  description: string;
  color: string;
  icon: string;
}) {
  return <div className="size-32 bg-black">adfasdf</div>;
}
